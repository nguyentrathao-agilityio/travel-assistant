export const VISIT_DURATION_MIN: Record<string, number> = {
  attraction: 60,
  restaurant: 60,
  cafe: 30,
  activity: 120,
  nightlife: 90,
  shopping: 45,
};

export const ROUTE_MODE_MAP: Record<string, string> = {
  walk: 'walk',
  bike: 'motorbike',
  car: 'taxi',
};

export const DEFAULT_STOPS = 5;
export const MAX_STOPS_LIMIT = 8;
