{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/agent/Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "preDeployCommand": "pnpm --filter agent db:seed-knowledge:deploy"
  }
}
